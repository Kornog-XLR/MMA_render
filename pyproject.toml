[project]
name = "mma-render"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "daratumumab", email = "dara.chean@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "dash>=4.4.1",
    "dash-bootstrap-components>=2.0.4",
    "pandas>=3.0.5",
    "plotly>=7.1.0",
]

[project.scripts]
mma-render = "mma_render:main"

[build-system]
requires = ["uv_build>=0.12.14,<0.13.0"]
build-backend = "uv_build"
